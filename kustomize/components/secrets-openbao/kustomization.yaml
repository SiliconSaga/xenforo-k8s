apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Opt-in secrets: resolve `xenforo-secrets` from OpenBAO via ESO.
# Replaces overlays/plain/secret.yaml (do not include both — same name).
resources:
  - externalsecret.yaml
